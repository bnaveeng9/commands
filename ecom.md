> https://code.visualstudio.com/docs/languages/markdown
> https://github.com/machadop1407/ecommerce-nextjs-fullstack-app/tree/main
> 

# shadcn components for ui
```node
npx shadcn@latest init
```
### creates a lib folder and 
creates a lib/util.js folder afer the above shadcn command 

``` javascript
import { clsx, type ClassValue } from "clsx"
import { twMerge } from "tailwind-merge"

export function cn(...inputs: ClassValue[]) {
  return twMerge(clsx(inputs))
}
```

# install stripe

```javascript
npm install stripe
```

# shadcn install for UI component
```javascript
npx shadcn@latest add button
npx shadcn@latest add card
npm install zustand

```

# herosicon icon library from react
```javascript
npm install @heroicons/react

npm i next@latest react@latest react-dom@latest eslint-config-next@latest

```



